{
  "group": "HYVR — Brand & Content",
  "definitions": [
    {
      "title": "Columns",
      "id": "columns",
      "plugins": {
        "xwalk": {
          "page": {
            "resourceType": "core/franklin/components/block/v1/block",
            "template": {
              "name": "Columns",
              "model": "columns",
              "filter": "columns"
            }
          }
        }
      }
    }
  ],
  "models": [
    {
      "id": "columns",
      "fields": [
        {
          "component": "text",
          "name": "note",
          "label": "One row; each cell becomes a column",
          "readonly": true
        }
      ]
    }
  ],
  "filters": [
    {
      "id": "columns",
      "components": [
        "text",
        "image",
        "button",
        "hero",
        "stats",
        "quote"
      ]
    }
  ]
}